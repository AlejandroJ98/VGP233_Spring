using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerMovement : MonoBehaviour
{
    //playerMovement is in charge of rocket movement, including up down left and down, Since the rocket will automatically falling down due to
    //gravity, there will be no down movement. Also, this class will contains movement speed of rocket, rotation of rocket and rigidbody etc.
   
    // Start is called before the first frame update

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
